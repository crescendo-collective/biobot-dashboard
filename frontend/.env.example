# Local overrides — copy to .env and fill in values

# API Gateway base URL (omit to use /api proxy in dev)
# VITE_API_BASE_URL=http://127.0.0.1:3000

# Use bundled sample JSON instead of calling the API
VITE_USE_MOCK_DATA=true
